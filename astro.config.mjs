import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://roipeker.github.io/cabanaslanina',
  base: '/cabanaslanina',
  trailingSlash: 'always',
  compressHTML: true,
});
